# 🌐 將 Worker 綁定到自訂域名

## ⚠️ 為什麼需要這樣做？

MailChannels 不允許從 `workers.dev` 域名發送郵件，必須使用自訂域名。

---

## 📋 步驟 1：在 Cloudflare Dashboard 添加自訂域名

### 1. 登入 Cloudflare Dashboard
訪問：https://dash.cloudflare.com

### 2. 進入 Workers & Pages
- 點擊左側選單的 **Workers & Pages**

### 3. 選擇你的 Worker
- 找到並點擊 `apcs-auth-api`

### 4. 添加自訂域名
1. 點擊 **Settings** 標籤
2. 找到 **Domains & Routes** 或 **Triggers** 部分
3. 在 **Custom Domains** 區域點擊 **Add Custom Domain**
4. 輸入：`api.apcs.launchdock.app`
5. 點擊 **Add Custom Domain**

### 5. 等待 DNS 設置
Cloudflare 會自動添加必要的 DNS 記錄，通常幾秒鐘就完成。

---

## ✅ 驗證設置

### 檢查 Worker 是否可以通過自訂域名訪問

```bash
curl https://api.apcs.launchdock.app/health
```

應該返回：
```json
{
  "status": "ok",
  "message": "APCS Auth API is running"
}
```

---

## 🧪 測試 Email 發送

使用**新的自訂域名**測試：

```bash
curl -X POST https://api.apcs.launchdock.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"your-email@gmail.com","password":"test123456","name":"測試"}'
```

應該看到：
```json
{
  "success": true,
  "message": "註冊成功！請查收驗證 Email",
  "userId": 18,
  "emailSent": true  ← 這裡應該是 true！
}
```

---

## 🔄 更新前端 API URL

設置完成後，需要更新前端的 API URL：

### 在 auth-client.js 中

將：
```javascript
const API_URL = 'https://apcs-auth-api.589411.workers.dev';
```

改為：
```javascript
const API_URL = 'https://api.apcs.launchdock.app';
```

---

## 📊 完整域名結構

設置完成後，你的域名結構：

- **主域名**: `launchdock.app`
- **網站**: `apcs.launchdock.app`
- **API**: `api.apcs.launchdock.app`
- **Email**: `noreply@apcs.launchdock.app`

---

## 🎯 預計時間

- **添加自訂域名**: 2 分鐘
- **DNS 生效**: 幾秒鐘（Cloudflare 自動處理）
- **測試**: 1 分鐘

**總計**: 3-5 分鐘 ⏱️

---

**現在就去 Cloudflare Dashboard 添加自訂域名吧！** 🚀

這是讓 MailChannels 正常工作的最後一步！
